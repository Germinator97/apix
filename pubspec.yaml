name: apix

description: "Production-ready Flutter/Dart API client with auth refresh queue, exponential retry, smart caching and built-in Sentry. Powered by Dio."

version: 0.0.1

homepage: https://github.com/Germinator97/apix

repository: https://github.com/Germinator97/apix

issue_tracker: https://github.com/Germinator97/apix/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0
  crypto: ^3.0.0
  sentry_flutter: ^8.0.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.0
